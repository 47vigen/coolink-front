import { brands } from './brands'
import { services } from './services'
import { contacts } from './contacts'
import { animate, borderStyle, direction, rounded, type, types } from './customize'

export { brands, services, contacts, animate, borderStyle, direction, rounded, type, types }
